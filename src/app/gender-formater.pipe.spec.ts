import { GenderFormaterPipe } from './gender-formater.pipe';

describe('GenderFormaterPipe', () => {
  it('create an instance', () => {
    const pipe = new GenderFormaterPipe();
    expect(pipe).toBeTruthy();
  });
});
